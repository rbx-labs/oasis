name: oasis

services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: oasis_app
    volumes:
      - ./app:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "8080:8080"
    command: yarn dev --port 8080
    networks:
      - network

  public:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: oasis_public
    volumes:
      - ./backend:/backend
    ports:
      - "3000:3000"
    command: >
      sh -c "
        if [ -d '/backend/db/migrations/versions' ] && [ -n "$(ls -A /backend/db/migrations/versions/)" ]; then
          echo 'Applying migrations...' &&
          alembic upgrade head;
        else
          echo 'No migrations found. Please create migrations first.';
        fi &&
        uvicorn main_public:app --host 0.0.0.0 --port 3000 --reload
      "
    env_file:
      - ./backend/.env
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    networks:
      - network
    depends_on:
      - db

  internal:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: oasis_internal
    volumes:
      - ./backend:/backend
    ports:
      - "3001:3001"
    command: uvicorn main_internal:app --host 0.0.0.0 --port 3001 --reload
    env_file:
      - ./backend/.env
    networks:
      - network
    depends_on:
      - public

  db:
    image: postgres:15
    container_name: oasis_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - network
    env_file:
      - ./backend/.env

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    container_name: oasis_admin
    volumes:
      - ./admin:/admin
      - /admin/node_modules
      - /admin/.next
    ports:
      - "8081:8081"
    command: yarn dev --port 8081
    networks:
      - network

  wallet:
    build:
      context: ./wallet
      dockerfile: Dockerfile
    container_name: oasis_wallet
    volumes:
      - ./wallet:/wallet
    ports:
      - "3002:3002"
    command: uvicorn main:app --host 0.0.0.0 --port 3002 --reload
    networks:
      - network

  brain:
    build:
      context: ./brain
    container_name: oasis_brain
    volumes:
      - ./brain:/brain
    command: python main.py
    networks:
      - network

  llm:
    build:
      context: ./llm
      dockerfile: Dockerfile
    container_name: oasis_llm
    ports:
      - "11434:11434"
    networks:
      - network

  nginx:
    image: nginx:alpine
    container_name: oasis_nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - app
      - public
      - internal
      - admin
      - wallet
      - llm
    networks:
      - network

  ngrok:
    image: ngrok/ngrok:latest
    container_name: oasis_ngrok
    volumes:
      - ./ngrok.yml:/etc/ngrok.yml
    ports:
      - "4040:4040"
    command: start --all --config=/etc/ngrok.yml
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    env_file:
      - ./backend/.env
    depends_on:
      - nginx
    networks:
      - network

volumes:
  postgres_data:

networks:
  network:
    driver: bridge
